<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.ItemDao">

<insert id="insertItem" parameterType="Item">
  INSERT INTO vote_item (VI_ID,VO_ID,VS_ID,VU_USER_ID) VALUES (NULL,#{VO_ID},#{VS_ID},#{VU_USER_ID})
</insert>

<select id="isEmpty" resultType="Integer" parameterType="Vote">
 SELECT COUNT(*) FROM vote_item,vote_user WHERE
vote_item.VU_USER_ID=vote_user.VU_USER_ID AND VS_ID=#{VS_ID}
</select>
</mapper>